apiVersion: opentelemetry.io/v1beta1
kind: OpenTelemetryCollector
metadata:
  name: otel-gateway
  namespace: monitoring
spec:
  mode: statefulset
  replicas: 1
  serviceAccount: otel-collector
  image: ghcr.io/open-telemetry/opentelemetry-collector-releases/opentelemetry-collector-contrib:0.161.0@sha256:fd328de2552466ad78385e1b1289c3f2402b1c45f265b252aab1955b42845ac1

  # Target Allocator for dynamic discovery on Gateway (Requires StatefulSet mode)
  targetAllocator:
    enabled: true
    image: ghcr.io/open-telemetry/opentelemetry-operator/target-allocator:v0.159.0@sha256:03b70585d71e862edbf311f4592a5e876b056b5f5009ada5345c5625e9ac0382
    serviceAccount: otel-collector
    prometheusCR:
      enabled: true
      serviceMonitorSelector:
        matchLabels:
          app.kubernetes.io/name: kube-state-metrics
      podMonitorSelector:
        matchLabels:
          app.kubernetes.io/name: kube-state-metrics

  config:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

      # Gateway now handles dynamic scraping
      prometheus:
        config:
          scrape_configs:
            - job_name: 'otel-collector'
              scrape_interval: 20s
              static_configs:
                - targets: ['0.0.0.0:8888']
        target_allocator:
          endpoint: http://otel-gateway-targetallocator:80
          interval: 30s

    processors:
      memory_limiter:
        check_interval: 1s
        limit_percentage: 80
        spike_limit_percentage: 10

      batch:
        send_batch_size: 2048
        timeout: 10s

      resource:
        attributes:
          - key: cluster.name
            value: "talos-homelab"
            action: upsert
          - key: deployment.environment
            value: "homelab"
            action: upsert

    exporters:
      # Primary Metrics Storage
      prometheusremotewrite/victoriametrics:
        endpoint: "http://vmsingle-vm-shared:8428/api/v1/write"
        resource_to_telemetry_conversion:
          enabled: true
        external_labels:
          cluster: talos-homelab

      debug:
        verbosity: normal

    service:
      pipelines:
        metrics:
          receivers: [otlp, prometheus]
          processors: [memory_limiter, resource, batch]
          exporters: [prometheusremotewrite/victoriametrics]

  resources:
    limits:
      cpu: 500m
      memory: 1Gi
    requests:
      cpu: 200m
      memory: 256Mi
